# Environment / secrets
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*.keystore
id_rsa*

# OS / editor cruft
Thumbs.db
desktop.ini
.DS_Store
.vscode/
.idea/

# Playwright MCP output (traces, screenshots, downloads)
.playwright-mcp/

# Vendored agent-skill payloads (installed locally via `npx skills add`;
# skills-lock.json records what they should be). Several MB, not source.
.agents/
.claude/skills/
